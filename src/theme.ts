import {DefaultTheme} from 'react-native-paper';

export default {
  ...DefaultTheme,
  dark: false,
  roundness: 3,
  colors: {
    ...DefaultTheme.colors,
    primary: '#15212b',
    accent: '#fff',
    background: '#fff',
  },
};
